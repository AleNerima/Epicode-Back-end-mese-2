@model List<DittaSpedizioniApp.Models.Spedizione>

<h1>Elenco delle Spedizioni</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Nuova Spedizione</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Numero Identificativo</th>
            <th>Data Spedizione</th>
            <!-- Aggiungi altre colonne qui se necessario -->
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var spedizione in Model)
        {
            <tr>
                <td>@spedizione.NumeroIdentificativo</td>
                <td>@spedizione.DataSpedizione.ToShortDateString()</td>
                <!-- Aggiungi altre colonne qui se necessario -->
                <td>
                    <a asp-action="Details" asp-route-id="@spedizione.IdSpedizione">Dettagli</a> |
                    <a asp-action="Edit" asp-route-id="@spedizione.IdSpedizione">Modifica</a> |
                    <a asp-action="Delete" asp-route-id="@spedizione.IdSpedizione">Elimina</a>
                </td>
            </tr>
        }
    </tbody>
</table>

